version: "3.8"

services:
  backend:
    image: storex-backend:v4
    environment:
      - ENV=dev
    depends_on:
      - postgres

  postgres:
    image: postgres:13
    environment:
      - POSTGRES_DB=storex_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
